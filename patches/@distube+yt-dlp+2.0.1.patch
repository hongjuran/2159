diff --git a/node_modules/@distube/yt-dlp/dist/index.js b/node_modules/@distube/yt-dlp/dist/index.js
index b17b126..a3c4468 100644
--- a/node_modules/@distube/yt-dlp/dist/index.js
+++ b/node_modules/@distube/yt-dlp/dist/index.js
@@ -73,16 +73,24 @@ var args = /* @__PURE__ */ __name((url, flags = {}) => [url].concat((0, import_d
 var json = /* @__PURE__ */ __name((url, flags, options) => {
   const process2 = (0, import_node_child_process.spawn)(YTDLP_PATH, args(url, flags), options);
   return new Promise((resolve, reject) => {
-    let output = "";
+    let stdout = "";
+    let stderr = "";
     process2.stdout?.on("data", (chunk) => {
-      output += chunk;
+      stdout += chunk;
     });
     process2.stderr?.on("data", (chunk) => {
-      output += chunk;
+      stderr += chunk;
     });
     process2.on("close", (code) => {
-      if (code === 0) resolve(JSON.parse(output));
-      else reject(new Error(output));
+      if (code === 0) {
+        try {
+          const jsonStart = stdout.indexOf("{");
+          const jsonEnd = stdout.lastIndexOf("}");
+          resolve(JSON.parse(jsonStart >= 0 && jsonEnd >= jsonStart ? stdout.slice(jsonStart, jsonEnd + 1) : stdout));
+        } catch (err) {
+          reject(new Error(`${err.message}\n${stdout}\n${stderr}`));
+        }
+      } else reject(new Error(stderr || stdout));
     });
     process2.on("error", reject);
   });
@@ -144,7 +152,6 @@ var YtDlpPlugin = class extends import_distube.PlayableExtractorPlugin {
     const info = await json(url, {
       dumpSingleJson: true,
       noWarnings: true,
-      noCallHome: true,
       preferFreeFormats: true,
       skipDownload: true,
       simulate: true
@@ -174,7 +181,6 @@ var YtDlpPlugin = class extends import_distube.PlayableExtractorPlugin {
     const info = await json(song.url, {
       dumpSingleJson: true,
       noWarnings: true,
-      noCallHome: true,
       preferFreeFormats: true,
       skipDownload: true,
       simulate: true,
